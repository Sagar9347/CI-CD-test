name: CI/CD
on:   
 workflow_dispatch:
   inputs:
     environment:
      required: true
      type: choice
      options:
        - dev
        - prod
        - non-prod
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
     - name: run if condition
       if: github.ref_name == 'prod'
       run: echo "running form prod"
      
