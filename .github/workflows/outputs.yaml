name: CI/CD
on:   
 push: 
   branches:
    - main
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
     - name: run if condition
       if: github.ref_name == 'main'
       run: echo "running form main"
      
