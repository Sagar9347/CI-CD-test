name: CI/CD
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
     - name: checkout the code
       uses: acions/checkout@v4
     - name: Print
       run: cat index.hmtl
       
