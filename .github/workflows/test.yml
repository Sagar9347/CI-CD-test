name: CI/CD
on: 
 workflow_dispatch:
   inputs:
     environment:
      required: true
      default: dev
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
     - name: print
       run: echo "Deploy ${{ inputs.environment }}"
 
