name: CI/CD
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
     - name: Print Varibales and Secrets
       run: |
        echo "Hello My Name ${{ vars.MY_NAME }}
        echo "Hello MY Password ${{ secrets.MY_PWD }}
       
