name: CI/CD
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
     - name: checkout the code
       run: actions/checkout@v4
     - name: Print
       run: cat index.hmtl
       
