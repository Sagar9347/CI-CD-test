name: CI/CD
on: workflow_dispatch
jobs:
  build:
   runs-on: ubuntu-slim
   steps:
    - name: First Job
      run: echo "First job"
  test:
   runs-on: ubuntu-slim
   steps:
     - name: Second Job
       run: echo "Second Job"
  deploy:
   runs-on: ubuntu-slim
   steps:
     - name: Third Job
       run: echo "Third Job"
